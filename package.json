{
  "name": "const-arch-app",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "prebuild": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run frontend:build && npm run backend:build",
    "frontend:build": "npm run build --prefix frontend",
    "backend:build": "echo 'Backend build step (customize if needed)'",
    "start": "node app.js",
    "serve": "npm run build && npm start",
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@redux-devtools/extension": "^3.3.0",
    "@reduxjs/toolkit": "^2.7.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "firebase-admin": "^13.2.0",
    "react-redux": "^9.2.0",
    "react-router-dom": "^7.5.0",
    "vite": "^6.2.6"
  },
  "devDependencies": {
    "chai": "^5.2.0",
    "jest": "^29.7.0",
    "mocha": "^11.1.0",
    "supertest": "^7.1.0"
  }
}
